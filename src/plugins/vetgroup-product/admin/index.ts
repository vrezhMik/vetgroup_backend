// src/plugins/vetgroup-product/admin/index.ts
export default {
  register(app) {
    // Optional: Add custom UI components here
  },

  bootstrap(app) {
    // Optional: Run code when Strapi starts
  },
};
